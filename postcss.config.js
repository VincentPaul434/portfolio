// Tailwind is handled by the @tailwindcss/vite plugin (see vite.config.js).
// This empty config exists only to stop PostCSS from walking up the directory
// tree and picking up an unrelated config outside the project.
export default {
  plugins: {},
}
